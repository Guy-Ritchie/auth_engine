"""Placeholder so pytest finds at least one test during Phase 0."""


def test_phase_zero_scaffold_is_alive() -> None:
    """Confirms the test suite is discoverable. Remove after Phase 1."""
    assert True
